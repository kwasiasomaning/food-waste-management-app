name: CI

on:
  push:
    branches:
      - main
      - cursor/tonight-food-waste-app-46af
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm test -- --watchman=false
      - run: npm run typecheck
